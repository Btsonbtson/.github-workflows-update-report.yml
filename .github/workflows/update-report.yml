name: PRO Daily Report Update (05:55 Greece)

on:
  schedule:
    - cron: '55 2 * * *'  # 05:55 Greece (UTC+3)
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install feedparser beautifulsoup4 requests ics pytz
          
      - name: Install dependencies
        run: |
         pip install feedparser beautifulsoup4 requests python-dateutil pytz ics

      - name: Run Python Script to Generate Report
        run: python daily_news_agent_v3_PRO.py

      - name: Commit and Push index.html
        run: |
          git config --global user.name 'PRO Bot'
          git config --global user.email 'actions@github.com'
          git add index.html
          git commit -m "📄 Daily report update: $(date '+%Y-%m-%d %H:%M:%S')" || echo "No changes to commit"
          git push

      - name: Send WhatsApp Reminder via CallMeBot
        run: |
          curl -s -G --data-urlencode "text=🗞️ Το report ενημερώθηκε:\nhttps://btsonbtson.github.io/NewsBrief/" \
            --data-urlencode "apikey=2318162" \
            --data-urlencode "phone=+306932377969" \
            "https://api.callmebot.com/whatsapp.php"
